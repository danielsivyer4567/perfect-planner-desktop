{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-read-only",
  "description": "Validated board reads, the app-local fenced supervisor, local repository messaging, and identity-fenced approval observation. External task routing stays native-only.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-board-discovery",
    "allow-collision-assessor",
    "allow-control-plane",
    "allow-orchestrator-pipeline"
  ]
}
